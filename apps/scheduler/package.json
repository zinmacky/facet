{
	"name": "@facet/scheduler",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"migrate:local": "wrangler d1 migrations apply facet-jobs --local",
		"migrate:remote": "wrangler d1 migrations apply facet-jobs --remote"
	},
	"dependencies": {
		"@facet/contract": "workspace:*",
		"hono": "^4.12.28",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240925.0",
		"typescript": "^5.6.2",
		"vitest": "^3.2.7",
		"wrangler": "^4.107.0"
	}
}
