{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "メインウィンドウの権限。Phase 1 の疎通確認用最小権限(core:default)に加え、renderer 配線(Phase 2 最終接続)で元動画/書き出し先フォルダのネイティブ選択に使う dialog プラグインの既定権限を追加する。",
	"windows": ["main"],
	"permissions": ["core:default", "dialog:default"]
}
