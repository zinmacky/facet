node_modules/
dist/
dist-tsnode/
*.tsbuildinfo
.turbo/
.playwright-mcp/
*.log
.DS_Store

# env / secrets
.env
.env.*
!.env.example
.dev.vars

# cloudflare
.wrangler/

# build outputs
apps/studio/web/dist/
coverage/

# studio 書き出し一時ディレクトリ
.work/

# Rust(apps/desktop)
target/
apps/desktop/src-tauri/gen/

# Phase 4: リリース用 LGPL FFmpeg DLL ステージング(scripts/fetch-ffmpeg-lgpl.ps1 が生成)。
apps/desktop/src-tauri/ffmpeg-dist/
.cache/

# Claude Code: 個人の permission 蓄積(共有は .claude/settings.json 側)
.claude/settings.local.json
# Claude Code: サブエージェント用の一時 git worktree(コミット対象外)
.claude/worktrees/
